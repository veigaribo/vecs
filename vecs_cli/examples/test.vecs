component transform {
  double x;
  double y;
};

component render {
  texture_t texture;
};

event mouse_click {
  double x;
  double y;
  uint8_t button;
};

system move { transform };
system render { transform; render };

// state main {
//   components {
//     transform { max 100 };
//     render { max 100 };
//   };
//
//   systems {
//     { move };
//     { render };
//   };
// };
